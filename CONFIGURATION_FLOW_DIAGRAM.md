# Dynamic Configuration Flow - Visual Guide

## 🔄 Backend Configuration Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    Application Startup                       │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│         backend/src/config/env.js loads                      │
│  - Checks NODE_ENV environment variable                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
              ┌─────────────┴─────────────┐
              ↓                           ↓
    ┌──────────────────────┐   ┌──────────────────────┐
    │  NODE_ENV not set    │   │  NODE_ENV is set     │
    │  or = 'development'  │   │                      │
    └──────────────────────┘   └──────────────────────┘
              ↓                           ↓
    ┌──────────────────────┐   ┌──────────────────────┐
    │  Load backend/.env   │   │ Check which value:   │
    │  (local database)    │   │ - 'development'      │
    └──────────────────────┘   │ - 'production'       │
              ↓                │ - 'staging'          │
    ┌──────────────────────┐   └──────────────────────┘
    │  PGHOST=localhost    │              ↓
    │  PGPORT=5432         │   ┌──────────────────────┐
    │  No SSL              │   │ Load corresponding:  │
    │  Development defaults│   │ backend/.env.prod    │
    └──────────────────────┘   │ backend/.env.staging │
              ↓                └──────────────────────┘
         DATABASE                    ↓
         READY           ┌──────────────────────┐
                         │ Validate required    │
                         │ variables (prod only)│
                         │ Throw error if       │
                         │ missing              │
                         └──────────────────────┘
                                   ↓
                         ┌──────────────────────┐
                         │ Enable SSL           │
                         │ (PGSSLMODE=require)  │
                         │ Use prod credentials │
                         └──────────────────────┘
                                   ↓
                              DATABASE
                              READY
```

---

## 🌐 Frontend Configuration Flow

```
┌─────────────────────────────────────────────────────────────┐
│               Frontend Build/Run Process                     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│         src/config/api.js getApiBaseUrl()                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
         ┌──────────────────┬──────────────────┐
         ↓                  ↓                  ↓
    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
    │ Check for   │  │ Check if    │  │ Check       │
    │ Explicit    │  │ Development │  │ Relative    │
    │ URL env var │  │ Environment │  │ URL flag    │
    └─────────────┘  └─────────────┘  └─────────────┘
         ↓                  ↓                  ↓
    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
    │ VITE_API_   │  │ import.meta │  │ VITE_USE_   │
    │ BASE_URL    │  │ .env.DEV    │  │ RELATIVE_   │
    │ set?        │  │ true?       │  │ API_URL     │
    └─────────────┘  └─────────────┘  └─────────────┘
         │ YES              │ YES              │ true
         ↓                  ↓                  ↓
    ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
    │ Use that    │  │ Use         │  │ Use empty   │
    │ explicit    │  │ localhost   │  │ string ''   │
    │ URL         │  │ :3001       │  │ for API     │
    │             │  │             │  │ calls       │
    └─────────────┘  └─────────────┘  └─────────────┘
         │                  │                  │
         └──────────────────┼──────────────────┘
                            ↓
        ┌──────────────────────────────────┐
        │  Use API_BASE_URL for all API    │
        │  requests in getApiRequest()     │
        └──────────────────────────────────┘
                            ↓
        ┌──────────────────────────────────┐
        │  fetch(`${API_BASE_URL}/endpoint`)
        │  Automatically uses correct URL  │
        └──────────────────────────────────┘
```

---

## 📁 Configuration File Precedence

### Backend (from highest to lowest priority)

```
1. System Environment Variables
   ├─ export PGHOST=prod.example.com
   ├─ heroku config:set PGHOST=...
   └─ Docker -e PGHOST=...
        ↓
2. .env File (NODE_ENV-specific)
   ├─ backend/.env (development)
   ├─ backend/.env.production
   └─ backend/.env.staging
        ↓
3. Code Defaults (Development only)
   ├─ localhost:5432
   ├─ postgres/postgres
   └─ No SSL
```

### Frontend (from highest to lowest priority)

```
1. Build-time Environment Variables
   ├─ npm run build (reads NODE_ENV)
   ├─ VITE_API_URL=... npm run build
   └─ Platform build settings
        ↓
2. .env File (NODE_ENV-specific)
   ├─ .env (development)
   ├─ .env.production
   └─ .env.staging
        ↓
3. Runtime Defaults
   ├─ window.location.origin
   ├─ localhost:3001 (dev)
   └─ Current domain (prod)
```

---

## 🚀 Deployment Flow

```
┌─────────────────────────────────────────────────────────────┐
│               Choose Deployment Platform                     │
└─────────────────────────────────────────────────────────────┘
        ↓
    ┌───┴───┬────────┬────────┬──────────┬─────────┐
    ↓       ↓        ↓        ↓          ↓         ↓
┌──────┐ ┌────┐ ┌──────┐ ┌────────┐ ┌──────┐ ┌──────┐
│Heroku│ │AWS │ │Railway│ │Docker │ │Render│ │VPS  │
└──────┘ └────┘ └──────┘ └────────┘ └──────┘ └──────┘
    │       │        │        │          │         │
    ↓       ↓        ↓        ↓          ↓         ↓
┌────────────────────────────────────────────────────────────┐
│          Set Environment Variables                          │
│  NODE_ENV = production                                      │
│  PGHOST = your-database-host                               │
│  PGPASSWORD = secure_password                              │
│  VITE_API_URL = https://your-domain.com                    │
│  + Other required vars (JWT_SECRET, ERP_API_KEY, etc.)     │
└────────────────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────────────────┐
│  Application Starts                                         │
│  ├─ Backend: Reads NODE_ENV=production                     │
│  ├─ Loads: backend/.env.production or system vars          │
│  ├─ Validates all required variables                       │
│  ├─ Connects to: your-database-host                        │
│  ├─ Enables: SSL (PGSSLMODE=require)                       │
│  └─ Logs: Configuration loaded successfully                │
└────────────────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────────────────┐
│  Frontend Deployed                                          │
│  ├─ Built with: npm run build                              │
│  ├─ Reads: VITE_API_URL from env or .env.production        │
│  ├─ API calls to: https://your-domain.com                  │
│  └─ No hardcoded localhost!                                │
└────────────────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────────────────┐
│  Ready for Production! 🚀                                   │
│  ├─ ✅ Database connection verified                         │
│  ├─ ✅ API calls working                                    │
│  ├─ ✅ SSL enabled                                          │
│  ├─ ✅ Secrets secure                                       │
│  └─ ✅ No code changes needed                               │
└────────────────────────────────────────────────────────────┘
```

---

## 🔄 Local Development vs Production

```
┌─────────────────────────────────┐    ┌─────────────────────────────────┐
│    LOCAL DEVELOPMENT            │    │    PRODUCTION DEPLOYMENT        │
│  (npm run dev)                  │    │  (npm run build + deploy)       │
└─────────────────────────────────┘    └─────────────────────────────────┘
          │                                        │
    ┌─────┴─────┐                          ┌──────┴──────┐
    ↓           ↓                          ↓             ↓
┌─────────┐ ┌─────────┐            ┌────────────┐ ┌────────────┐
│Backend  │ │Frontend │            │Backend     │ │Frontend    │
│         │ │         │            │            │ │            │
│NODE_ENV │ │VITE_API │            │NODE_ENV=   │ │VITE_API_URL│
│=dev     │ │_URL=    │            │production  │ │=https://   │
│         │ │local:   │            │            │ │your-domain │
│Database │ │3001     │            │Database    │ │            │
│=local   │ │         │            │=prod       │ │ API Calls: │
│         │ │SSL:NO   │            │SSL:REQUIRE │ │prod domain │
│SSL:NO   │ │         │            │            │ │            │
└─────────┘ └─────────┘            └────────────┘ └────────────┘
     │           │                        │             │
     └─────┬─────┘                        └──────┬──────┘
           │                                    │
           ↓                                    ↓
    ┌─────────────┐                    ┌─────────────┐
    │ Both on     │                    │ Frontend    │
    │ localhost   │                    │ calls       │
    │ Port 5173   │                    │ Backend     │
    │ Port 3001   │                    │ at Prod URL │
    └─────────────┘                    │ Secure!     │
                                        └─────────────┘
```

---

## ✅ Verification Checklist by Environment

### Development (npm run dev)

```
Backend Startup Output:
✅ "NODE_ENV: development"
✅ "Database: postgres@localhost:5432/vms"
✅ "Server running on port 3001"

Frontend Console:
✅ fetch calls to: http://localhost:3001
✅ No CORS errors
✅ API responses successful
```

### Production (Deployed)

```
Backend Logs:
✅ "NODE_ENV: production"
✅ "Database: prod_user@your-domain.com:5432/vms_prod"
✅ "SSL: enabled"
✅ "Server running on port 3001"

Frontend DevTools:
✅ fetch calls to: https://api.yourdomain.com
✅ No CORS errors
✅ API responses successful
✅ No localhost references
```

---

## 🎯 Key Takeaways

| Aspect            | Development       | Production                          |
| ----------------- | ----------------- | ----------------------------------- |
| **Config Source** | backend/.env      | backend/.env.production or env vars |
| **Database Host** | localhost         | Your prod server                    |
| **SSL Mode**      | disable           | require                             |
| **API Base URL**  | localhost:3001    | your-domain.com                     |
| **Secrets**       | Default values OK | Must be strong/unique               |
| **Validation**    | Warnings only     | Errors if missing                   |
| **Code Changes**  | None              | None! (only env vars)               |
| **Deployment**    | npm run dev       | Build & deploy                      |

---

**That's it!** Your system now dynamically configures based on NODE_ENV! 🎉
