# Frontend Environment Variables - Production

# API Configuration
# Production API URL - can be:
# 1. Absolute URL: https://api.yourdomain.com
# 2. Relative URL by setting VITE_USE_RELATIVE_API_URL=true (if same server)
# 3. Implicit: uses window.location.origin as base if not specified
VITE_API_URL=${VITE_API_URL}

# Use relative URLs for API - set to 'true' if frontend and backend
# are served from the same origin in production
VITE_USE_RELATIVE_API_URL=${VITE_USE_RELATIVE_API_URL}

# Supabase Configuration (if using Supabase)
# VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
# VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}
